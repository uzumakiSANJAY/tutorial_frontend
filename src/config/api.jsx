// src/config/api.js

const BASE_URL = "http://127.0.0.1:5000/api/v1"; // Change to your production URL when needed

export const API_ROUTES = {
  //   getMessage: `${BASE_URL}/message`,
  submitForm: `${BASE_URL}/mail/send-mail`,
  // Add more endpoints as needed
};
